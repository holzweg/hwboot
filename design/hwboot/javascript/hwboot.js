$(document).ready(function() {
    // nothing todo yet :-(

    // automatic tabs handling
    $('.tabs').tabs();

    // Setup twipsy
    $("*[rel=twipsy]").twipsy();

    // Setup twipsy
    $("*[rel=popover]").popover();

});
